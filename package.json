{
  "name": "skillswap-prototype",
  "version": "1.0.0",
  "description": "SkillSwap - Skill sharing platform prototype",
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html --ignore=my-skillshare-backend",
    "start": "live-server --port=3000"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "live-server": "^1.2.2",
    "node-fetch": "^3.3.2"
  }
}
